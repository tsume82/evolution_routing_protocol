package routing;
import core.Settings;
public class BestIndividual_59 extends ActiveRouter{ 
	public BestIndividual_59(Settings s) {
		super(s);
	}
	protected BestIndividual_59(BestIndividual_59 r) {
		super(r);
	}
	@Override
	public void update() {
		super.update();
if(!!(canStartTransfer() || isTransferring())){if(((isTransferring() || isTransferring()) || (isTransferring() || isTransferring()))){if(canStartTransfer()){this.tryAllMessagesToAllConnections();}}}
if(canStartTransfer()){if(((canStartTransfer() || isTransferring()) || (isTransferring() != canStartTransfer()))){exchangeDeliverableMessages();
exchangeDeliverableMessages();
exchangeDeliverableMessages();
exchangeDeliverableMessages();}}	}


	@Override
	public BestIndividual_59 replicate() {
		return new BestIndividual_59(this);
	}
}