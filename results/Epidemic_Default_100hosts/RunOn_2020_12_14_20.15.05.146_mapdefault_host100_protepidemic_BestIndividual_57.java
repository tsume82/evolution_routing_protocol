package routing;
import core.Settings;
public class BestIndividual_57 extends ActiveRouter{ 
	public BestIndividual_57(Settings s) {
		super(s);
	}
	protected BestIndividual_57(BestIndividual_57 r) {
		super(r);
	}
	@Override
	public void update() {
		super.update();
if(((!canStartTransfer() || !isTransferring()) != (!canStartTransfer() != (canStartTransfer() || canStartTransfer())))){if((isTransferring() || isTransferring())){if(isTransferring()){exchangeDeliverableMessages();}}
this.tryAllMessagesToAllConnections();
this.tryAllMessagesToAllConnections();
if(isTransferring()){exchangeDeliverableMessages();}}
if((canStartTransfer() || isTransferring())){if(canStartTransfer()){super.update();}}
if(isTransferring()){this.tryAllMessagesToAllConnections();}
exchangeDeliverableMessages();
super.update();
if(((canStartTransfer() != canStartTransfer()) != (isTransferring() != isTransferring()))){if((isTransferring() || canStartTransfer())){this.tryAllMessagesToAllConnections();
this.tryAllMessagesToAllConnections();}}	}


	@Override
	public BestIndividual_57 replicate() {
		return new BestIndividual_57(this);
	}
}