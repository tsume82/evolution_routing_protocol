package routing;
import core.Settings;
public class BestIndividual_61 extends ActiveRouter{ 
	public BestIndividual_61(Settings s) {
		super(s);
	}
	protected BestIndividual_61(BestIndividual_61 r) {
		super(r);
	}
	@Override
	public void update() {
		super.update();
if(!!((isTransferring() || isTransferring()) || isTransferring())){exchangeDeliverableMessages();
this.tryAllMessagesToAllConnections();
this.tryAllMessagesToAllConnections();
exchangeDeliverableMessages();
if((canStartTransfer() || canStartTransfer())){if(isTransferring()){return;}}}
if(((isTransferring() != isTransferring()) != !canStartTransfer())){if(canStartTransfer()){this.tryAllMessagesToAllConnections();}
exchangeDeliverableMessages();
super.update();}
exchangeDeliverableMessages();
exchangeDeliverableMessages();
exchangeDeliverableMessages();
super.update();
if(isTransferring()){exchangeDeliverableMessages();}
if(isTransferring()){return;}	}


	@Override
	public BestIndividual_61 replicate() {
		return new BestIndividual_61(this);
	}
}